import { Fragment } from "react";

const ItemListContainer = (props) => {

    return (
        <Fragment>
            <p>{props.greeting}</p>
        </Fragment>
    )
}

export default ItemListContainer;